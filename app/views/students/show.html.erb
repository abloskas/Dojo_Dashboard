<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Show Student</title>
</head>
<body>
    <h1> <%= @dojo.branch %>'s <%= @student.first_name %> <%= @student.last_name %> </h1>
    <h3>Student's Email: <%= @student.email%> </h3>
    <br>
    <h2>Cohort:</h2>
    <table style="border:1px solid black">
        <tr>
            <th>Student ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
        </tr>
        <% @dojo.students.where(created_at: (@student.created_at.beginning_of_day..@student.created_at.end_of_day)).where.not(id: @student.id).each do |s| %>
        <tr>
            <td><%= s.id %></td>
            <td><%= s.first_name %></td>
            <td><%= s.last_name %></td>
            <td><%= s.email %></td>
        </tr>
    <% end %>
    </table>  
    <br>
    <a href="/dojos/<%= @dojo.id %>/students">Back</a> | <a href="/dojos/<%= @dojo.id %>/students/<%=@student.id%>/edit">Edit</a>

   

</body>
</html>